{
  "$comment": [
    "T82. Registers the quantdesk MCP connector for the Claude CLI at the project level.",
    "",
    "This file is AN INSTANCE of the registration, not the interface. The interface is the",
    "command below: any MCP client that launches a stdio server can be pointed at the same",
    "process, and context/mcp-connector.md gives the generic invocation. Nothing in the tool",
    "definitions assumes a Claude-shaped client.",
    "",
    "DATABASE_URL_RO must authenticate as quantdesk_ro. There is deliberately no fallback to",
    "DATABASE_URL: the server refuses to start rather than run with write access, because a",
    "connector that works perfectly and is not read-only is the worst possible outcome.",
    "",
    "The ${VAR} form is expanded by the Claude CLI from the PROCESS ENVIRONMENT ONLY. It does",
    "not read this repo's .env, and it does not read .claude/settings.local.json's env block --",
    "both were tried on 2026-09-20 and the CLI still reported the variable missing. Set a real",
    "environment variable before launching the client:",
    "",
    "    setx QUANTDESK_RO_PASSWORD \"<value>\"      # Windows, then open a NEW terminal",
    "    export QUANTDESK_RO_PASSWORD=\"<value>\"    # POSIX",
    "",
    "It must match QUANTDESK_RO_PASSWORD in .env, which is what app/core/ro_role.py applies to",
    "the role at backend boot. POSTGRES_DB needs nothing: the :-gex default covers it."
  ],
  "mcpServers": {
    "quantdesk": {
      "command": "uv",
      "args": ["run", "--directory", "backend", "python", "-m", "app.mcp"],
      "env": {
        "DATABASE_URL_RO": "postgresql://quantdesk_ro:${QUANTDESK_RO_PASSWORD}@localhost:5432/${POSTGRES_DB:-gex}"
      }
    }
  }
}
